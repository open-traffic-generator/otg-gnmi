[pytest]
addopts = -s -v
markers =
    suite-e2e: marks tests as e2e since they're slower (deselect with '-m "not e2e"')
    suite-validation: validation tests
    suite-boundary: test boundary conditions
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
    ignore::DeprecationWarning